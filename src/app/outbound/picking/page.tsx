'use client';

import AppLayout from '@/components/AppLayout';
import PickingListContent from './components/PickingListContent';

export default function PickingListPage() {
  return (
    <AppLayout>
      <PickingListContent />
    </AppLayout>
  );
}
